<UserControl x:Class="TerrariaConstructor.Views.CharacteristicsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:TerrariaConstructor.Views"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <StackPanel>
        <TextBox Text="{Binding Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
        <Slider Maximum="500"
                Value="{Binding Health, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
        <TextBox Text="{Binding Health, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
        <TextBox Text="{Binding PlayTime, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
        <TextBox Text="100000"
                 Background="{Binding HairColor}"/>
        <TextBox Text="1000000"
                 Background="Fuchsia"/>
        <TextBox Text="10000000"/>
    </StackPanel>
</UserControl>
